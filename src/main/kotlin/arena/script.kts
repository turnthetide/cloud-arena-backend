package arena

//import arena.services.StandingsService
import java.util.*

//StandingsService.Score(UUID.randomUUID(), 10, intArrayOf(10, 9, 11))

println(UUID.randomUUID())
println(UUID.randomUUID())
println(UUID.randomUUID())
